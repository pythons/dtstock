from distutils.core import setup
setup(
  name = 'dtstock',
  packages = ['dtstock'], # this must be the same as the name above
  version = '0.1',
  description = 'A random test lib',
  author = 'Jun Shi',
  author_email = 'shijuns.sj@gmail.com',
  url = 'https://github.com/pythons/dtstock', # use the URL to the github repo
  download_url = 'https://github.com/pythons/dtstock/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['prophet', 'precasting'], # arbitrary keywords
  classifiers = [],
)